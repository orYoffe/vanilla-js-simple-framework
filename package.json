{
  "name": "signals",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "tsc && vite build --outDir ./docs",
    "preview": "vite preview --outDir ./docs"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.3.4"
  },
  "dependencies": {
    "dom-chef": "^5.1.1",
    "jstates": "1.0.0"
  }
}
